apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-webapp-sample.fullname" . }}
  labels: {{ include "go-webapp-sample.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{ include "go-webapp-sample.selectorLabels" . | nindent 4 }}
